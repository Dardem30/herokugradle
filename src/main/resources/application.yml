server:
 port: 8080
spring:
 jpa:
  database: postgresql
  show-sql: true
 datasource:
  driver-class-name: org.postgresql.Driver
  url: jdbc:postgresql//localhost:5050/
  username: postgres
  password: root
  name: test